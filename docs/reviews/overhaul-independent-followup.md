# Independent follow-up — acceptance withheld

The broad read-only reviewer timed out without a final summary. Findings below were independently checked in source by the parent, not accepted from its transcript. Initial implementation remains unaccepted.

## Verified test results

Parent ran bun run build successfully. Initial CI=true bun run test failed because new test/indexing-resume.test.ts imported bun:test. Parent corrected that one-line import to vitest and reran full suite: 23 files passed, 756 tests passed, 72 skipped, exit 0. Python full test/python suite: 52 passed, exit 0. These replace the delegate's inaccurate 118+/28 files report. Do not claim all architecture requirements implemented based on these tests.

## Required corrective pass

1. scripts/qmd_mlx/model_manager.py:39–185 has idle eviction and memory reporting but NO residency limit/admission/evict-before-load policy. Implement explicit conservative shared budget, injectable hardware measurements and deterministic 8/16/24/32/64GB tests. Resident weight budgets are not total activation memory guarantees; report limits honestly.
2. executor.py:174,195 uses strict heap priority and executes the entire callable. runtime.py:187–203 runs every micro-batch inside one callable. This neither yields to interactive requests between micro-batches nor prevents starvation. Implement bounded interleaving with original order/future/deadline preserved and deterministic tests proving interactive work runs between bulk micro-batches and bulk eventually runs under sustained interactive load.
3. runtime.py:175–182 loads and tokenizes BEFORE constructing deadline; executor.submit then grants another full timeout. One request budget must include load/tokenize/queue/micro-batches. Propagate one deadline/shared cancel event across all stages and validate actual TS request wiring; do not reset budgets per stage.
4. executor.py:157–168 claims idle callbacks run outside cv, but they execute inside `with self._cv`. Move callbacks out of lock and prevent admission/shutdown stalls; test with blocking callback. Recheck running state atomically under enqueue lock; shutdown must signal active cooperative work, not just queued futures.
5. model_manager.ensure_loaded checks READY outside execution then unconditionally queues load; concurrent cold requests can double-load. runtime.py:189–190 calls adapter.load directly bypassing manager state. Make state/load/eviction owner-serialized, single-flight and failure-recoverable. Touch idle timestamp on completion, not only start; test load/load, expiry/reload, unload vs queued work.
6. src/indexing/job.ts:78–80 guesses optional getEmbeddingDescriptor through any; :108–121 ignores options.signal; validateShadowTarget is lexical and does not resolve inode/symlink aliases. Checkpoint wrapper is not called by CLI (CLI still calls generateEmbeddings). Wire real descriptor and cancellation/checkpoint flows into supported API/CLI path without changing production. Prove restart resumes partially embedded documents and rejects stale chunker/model/content fingerprints. Path safety must reject live symlink/hardlink aliases. Pure save/load checkpoint tests are insufficient.
7. Correct docs/reviews/reliability-overhaul-results.md: remove unverified commit ID, invented-looking exact command transcripts and blanket fully implemented claims. Record exact executed results and outstanding requirements, including any original plan task deferred. Do not assert driver crashes were causally proven by source-level concurrency risk.

Complete remaining original plan audit too, especially embedding semantics, resource limits, request validation and benchmark evidence. Keep scope repository and temporary isolated fixtures only. No deployment, live/shadow index writes, model downloads, daemon changes, commits or pushes. Do not run full-corpus performance tests. Run real project build/Vitest/Python and add fault tests before reporting completion. Never suppress failures or convert a missing test into a pass.

# QMD MLX Embedding Server
# Usage: pip install -r scripts/mlx_server_requirements.txt

# Core MLX and Hugging Face stack
mlx>=0.22.0
mlx-lm>=0.12.0
transformers>=4.40.0
torch>=2.2.0
numpy>=1.26.0

# Optional: sentence-transformers fallback for non-native MLX models
sentence-transformers>=2.5.0

# Optional: faster loading of safetensors
safetensors>=0.4.0
